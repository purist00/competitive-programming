#include<vector>

using namespace std;

int main(){
vector<int>empty{};
vector<int> primes{2,3,5,7};

/**C++'s CTAD, compiler deduce the element type.
/**Indexes are distances (offset) from the 1st element of the array.
/** Last index is always - (N-1)
/** Arrays are contigous memory, arrays do not have any per-element overhead.
compiler can quickly calculate the address of any element in the array.
**/


/** direct initialization for a member of class type is diallowed.

vector<int> v1(8); // Not Allowed - Compile Error.
vector<int> v { vector<int>(8)}; //Ok



return 0;

}

/----------------------------------------------//----------------------------------------------/
#include <iostream>
#include <vector>
using namespace std;
int main(){
pair<string,int> pair1 = make_pair("Testing", 123);
cout<< pair1.first << " " << pair1.second <<endl;
















